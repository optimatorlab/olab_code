# olab_voice

Capture/transport-agnostic STT/TTS building blocks (dataclasses + `Protocol`
interfaces) for lab robotics and teaching projects. Depends on nothing
audio-hardware-specific by design; an `olab_audio`-backed adapter bridges
the two optionally.

**Status: scaffold only — not yet migrated.** Source will move from
`~/Projects/CoG/tts_practice_migration/src/ub_voice` (the canonical copy —
see the plan's "The `ub_voice` fork" section) per
[`docs/plans/olab_packages_reorg_plan.md`](../../docs/plans/olab_packages_reorg_plan.md),
Migration sequence step 2.
